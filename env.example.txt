# Environment Variables Template
# Copy this to .env.local for local development
# Add these to Vercel for production

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Option 1: Local MySQL (Development)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=annai_school

# Option 2: PlanetScale (Production) - Use this for Vercel
# DATABASE_URL=mysql://username:password@aws.connect.psdb.cloud/annai-school?ssl={"rejectUnauthorized":true}

# ============================================
# AUTHENTICATION (NextAuth.js)
# ============================================

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=your-super-secret-32-character-random-string-here

# Your deployment URL
# Local: http://localhost:3000
# Production: https://your-domain.vercel.app
NEXTAUTH_URL=http://localhost:3000

# ============================================
# FILE UPLOADS (Optional - UploadThing)
# ============================================

# Get these from https://uploadthing.com/
UPLOADTHING_SECRET=
UPLOADTHING_APP_ID=

# ============================================
# NOTES:
# ============================================
# 1. Never commit .env files to git
# 2. DATABASE_URL overrides individual DB_* variables
# 3. For Vercel, add all variables in dashboard under Settings > Environment Variables
# 4. NEXTAUTH_SECRET must be unique and random
# 5. NEXTAUTH_URL must match your domain exactly (no trailing slash)
